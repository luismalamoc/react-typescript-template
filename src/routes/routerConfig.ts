// React Router v7 configuration

// Enable future flags for React Router v7 compatibility
export const routerConfig = {
  // Wrap state updates in React.startTransition
  future: {
    v7_startTransition: true,
    v7_relativeSplatPath: true
  }
};
